# Invalid: forbidden secret-value field (AC-008). Checker must redact the literal.
version: 1

secrets:
  - id: SEC-BAD
    description: Must not embed credential literals
    enforcement_class: agent-instructed
    on_violation: instruct_stop
    value: SUPERSECRET_TOKEN_VALUE_NEVER_ECHO
