# Guided STRIDE row — classification guided; no wired command.
# Gate 7 must NOT upgrade this to enforced FAIL (AC-004).
version: 1
active: standard
profiles:
  standard:
    required: [spec, tests, review, threat-model]
  security-sensitive:
    required: [spec, threat-model, tests, sast, dependency-scan, review]
  release:
    required: [spec, tests, review, changelog, rollback-note]
