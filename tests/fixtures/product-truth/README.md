# Product Truth fixtures

Fixtures are local and deterministic. The Bats suite copies the canonical contract and governed trees into disposable roots, then applies one bounded mutation per AC. No fixture contains secrets, performs network access, or executes contract-provided content.
