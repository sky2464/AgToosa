# AgToosa Legacy Removed Workflow (fixture)

This file simulates a workflow that exists in an installed project but is no longer
shipped in the generator template. MAJOR migration plans should list it as `manual`.
