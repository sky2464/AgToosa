# progress-continuation-proof routing

Bare `next` / `do it` → execute `/agtoosa-next` with dispatch banner:

AgToosa Next → /agtoosa-spec (DEV-140) — idle backlog scan

help-next-disambiguation: `/agtoosa-help next` is preview-only; never substitute for PROGRESS utterances.
