---
description: AgToosa maintainer dogfood — Project Intake and Lifecycle Compass for the generator repo (docs/ paths).
globs: docs/**,template/**,lib/**,tests/**,.cursor/commands/agtoosa-*.md
alwaysApply: true
---

# AgToosa Maintainer Core Rules

You are in **Maintainer Dogfood Mode** — developing AgToosa itself (generator, `template/`, bats), not a downstream install.

Read `docs/agtoosa-maintainer.md` before generator, template, or bats changes. Do **not** run `agtoosa.sh` install against this repo; use `bash scripts/cursor-intake-fixture.sh` for downstream Cursor verification.

## Project Intake (freeform asks)

When the user omits `/agtoosa-*`, run **AgToosa Project Intake** per `docs/AgToosa_Agent.md` → **Project Intake Protocol** (read `docs/Context/workflow.md` Standing Corrections, classify soft/hard, expedite or hard-gate). Explicit `/agtoosa-*` bypasses intake ceremony.

## AgToosa Lifecycle Compass

When the user omits `/agtoosa-*`, run **AgToosa Lifecycle Compass** per `docs/AgToosa_Agent.md` (extends Project Intake). **Mandatory:** `bash agtoosa.sh --status-line [path]` before routing.

- Infer semantic intent (`PLAN`/`BUILD`/`REVIEW`/`SHIP`/`FIX`/`EXPLORE`/`TRACK`/`PROGRESS`) — not phrase matching. Reconcile with SYNC → **ANCHOR** (`spec` · `build` · `review` · `ship` · `none`).
- Soft: `Compass: soft → <phase>` · Hard: `**AgToosa Lifecycle Compass**` + `ANCHOR:` · Progress: `Compass: progress → /agtoosa-next`.
- **Continuation Context Contract:** pending interview Q → answer/momentum opt-in; post-closure or approval gate → `/agtoosa-next`; bare continuation + active cycle → `/agtoosa-next` — never raw phase slash on PROGRESS alone. See `docs/AgToosa_Agent.md`.
- Tributary: `serving <phase>` + `When done: return to /agtoosa-<phase>`.
- Explicit `/agtoosa-*` bypasses Compass ceremony. **Phase Stop:** never auto-chain Spec → Build → Review → Ship.

## On-demand assistance (`/agtoosa-next`)

- `/agtoosa-next` — primary sequential driver; see `docs/AgToosa_Next.md`. PROGRESS utterances (`next`, `okay`, `do it`, `continue`, etc.) dispatch Next per **Continuation Context Contract** — not raw `/agtoosa-build` or `/agtoosa-ship`.

## Cursor workflow command routing

Installed maintainer commands live in `.cursor/commands/agtoosa-*.md`. When the user types `/agtoosa-*`, execute the matching command file and `docs/AgToosa_*.md`. Do **not** route `/agtoosa-*` to `/create-skill` or generic skill flows.

## Key files (maintainer)

- `docs/agtoosa-maintainer.md` — Maintainer entry guide
- `docs/Master-Plan.md` — PM source of truth for AgToosa development
- `docs/AgToosa_Agent.md` — Full command reference and Project Intake
- `template/` — Source pack shipped to downstream installs (use `template/Docs/` paths there)
