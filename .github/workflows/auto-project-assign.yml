# Retired: classic GitHub Projects auto-assign (DEV-139)
#
# Replaced by `.github/workflows/agtoosa-issues-sync.yml` which mirrors
# `docs/Master-Plan.md` to GitHub Issues with AgToosa lifecycle labels.
#
# This workflow is intentionally disabled. Delete this file after one release cycle
# if no downstream forks depend on the classic Projects API behavior.

name: Auto-Assign to Project (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired — use agtoosa-issues-sync.yml instead."
