---
description: AgToosa goal clarification workflow
---

Read `Docs/AgToosa_Goal.md` and execute the `/agtoosa-goal` workflow.

Dispatch based on any arguments after the command: `project`, `story`, `check`, or `revise`.
