# AgToosa Init


## Windsurf workflow routing

This file is the native Windsurf project workflow for `/agtoosa-init`. When the user invokes `/agtoosa-init`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read `Docs/AgToosa_Init.md` and execute the `/agtoosa-init` workflow.
