# AgToosa Revert


## Windsurf workflow routing

This file is the native Windsurf project workflow for `/agtoosa-revert`. When the user invokes `/agtoosa-revert`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read `Docs/AgToosa_Revert.md` and execute the `/agtoosa-revert` workflow.
