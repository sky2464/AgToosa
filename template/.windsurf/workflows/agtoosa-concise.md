# AgToosa Concise


## Windsurf workflow routing

This file is the native Windsurf project workflow for `/agtoosa-concise`. When the user invokes `/agtoosa-concise`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read `Docs/AgToosa_Concise.md` and execute the `/agtoosa-concise` workflow.
