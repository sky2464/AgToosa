# AgToosa Task


## Windsurf workflow routing

This file is the native Windsurf project workflow for `/agtoosa-task`. When the user invokes `/agtoosa-task`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read `Docs/AgToosa_Task.md` and execute the `/agtoosa-task` workflow.
