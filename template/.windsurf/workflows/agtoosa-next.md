# AgToosa Next


## Windsurf workflow routing

This file is the native Windsurf project workflow for `/agtoosa-next`. When the user invokes `/agtoosa-next`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read `Docs/AgToosa_Next.md` and execute the next lifecycle dispatcher workflow.

**Mandatory:** run `bash agtoosa.sh --status-line [path] --route-hint --format json` before dispatch.

- No argument → dispatch and execute one lifecycle workflow
- `dry` → preview only
- `pick` → idle cold-start with user backlog pick

**Phase stop:** one phase per invocation. **Distinct from `/agtoosa-help next`:** this executes workflows.

<!-- AGTOOSA PRODUCT TRUTH START: command.next -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-next` (`command.next`)
- Canonical workflow: `Docs/AgToosa_Next.md`
- Modes: `full`, `dry`, `pick`, `fix`, `test`, `docs`
- Question budget: max 0; Quick max: 0
- Mutation class: `mixed`
- Approval gate: inherited
- Phase stop: required
- Lifecycle close: `none`
<!-- AGTOOSA PRODUCT TRUTH END: command.next -->
