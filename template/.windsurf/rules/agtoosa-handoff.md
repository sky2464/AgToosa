---
trigger: user_prompt
description: "AgToosa: export a handoff pack for an async or external agent (wave or task)"
---

When executing `/agtoosa-handoff` or any handoff pack export, follow `Docs/AgToosa_Handoff.md` precisely.

On successful completion, print this line verbatim: `✅ Done. Run /agtoosa-status to verify findings cleared.`
