# agtoosa-next

Read `Docs/AgToosa_Next.md` and execute the `/agtoosa-next` lifecycle dispatcher.

**Mandatory:** `bash agtoosa.sh --status-line [path] --route-hint --format json` before dispatch.

Sub-commands: `dry` (preview), `pick` (idle cold-start).

One lifecycle phase per invocation. Distinct from read-only `/agtoosa-help next`.

**Continuation Context Contract:** PROGRESS utterances (`next`, `okay`, `do it`, `continue`, etc.) → `/agtoosa-next` per `Docs/AgToosa_Agent.md` — pending interview Q → answer first. Step 1b blocked routing when review BLOCKED.

<!-- AGTOOSA PRODUCT TRUTH START: command.next -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-next` (`command.next`)
- Canonical workflow: `Docs/AgToosa_Next.md`
- Modes: `full`, `dry`, `pick`, `fix`, `test`, `docs`
- Question budget: max 0; Quick max: 0
- Mutation class: `mixed`
- Approval gate: none
- Phase stop: required
- Lifecycle close: `none`
<!-- AGTOOSA PRODUCT TRUTH END: command.next -->
