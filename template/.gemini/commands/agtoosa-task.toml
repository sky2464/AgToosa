description = "AgToosa: fast task capture to Master-Plan.md for bugs, chores, spikes, and fixes"
prompt = """

## Gemini command routing

This file is the native Gemini CLI command for `/agtoosa-task`. When the user invokes `/agtoosa-task`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read Docs/AgToosa_Task.md and execute the lightweight issue capture workflow.

Use any argument provided to pre-fill the issue type and description (e.g. "bug: null pointer in auth middleware"). If no argument is given, start from Step 1 and ask the user interactively.

On successful completion, print the dual-line phase close per Docs/AgToosa_Agent.md → Lifecycle Next-Step Contract
<!-- AGTOOSA PRODUCT TRUTH START: command.task -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-task` (`command.task`)
- Canonical workflow: `Docs/AgToosa_Task.md`
- Modes: `full`
- Question budget: max 3; Quick max: 0
- Mutation class: `write`
- Approval gate: required
- Phase stop: none
- Lifecycle close: `none`
<!-- AGTOOSA PRODUCT TRUTH END: command.task -->
"""
