description = "AgToosa: diagnose hard bugs — feedback-loop → reproduce → minimise → hypothesise → instrument → fix+regress"
prompt = """
Read Docs/AgToosa_Diagnose.md and execute the diagnosis workflow.

Dispatch based on any arguments provided after the command:
- /agtoosa-diagnose                → full 6-phase diagnosis loop
- /agtoosa-diagnose quick          → Phases 1-3 only (feedback loop, reproduce, minimise)
- /agtoosa-diagnose deep           → full loop with extended instrumentation
- /agtoosa-diagnose feedback-loop  → Phase 1 only: establish fast feedback loop
"""
