description = "AgToosa: smart lifecycle dispatcher — run the next spec/build/review/ship phase from Master-Plan state"
prompt = """

## Gemini command routing

This file is the native Gemini CLI command for `/agtoosa-next`. When the user invokes `/agtoosa-next`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read Docs/AgToosa_Next.md and execute the next lifecycle dispatcher workflow. Generated Project Mode — see Docs/AgToosa_Agent.md Operating Contexts.

**Mandatory:** run `bash agtoosa.sh --status-line [path] --route-hint --format json` before dispatch.

Dispatch based on arguments:
- /agtoosa-next       → full dispatch (state pulse → route → execute one lifecycle workflow)
- /agtoosa-next dry   → preview only; do not execute
- /agtoosa-next pick  → idle cold-start with user backlog pick → spec

**Phase stop:** one lifecycle command per invocation. Never auto-chain phases.

**Continuation Context Contract:** PROGRESS utterances (next, okay, do it, continue, etc.) route to /agtoosa-next per Docs/AgToosa_Agent.md — pending interview Q answers first.

**Distinct from /agtoosa-help next:** this command executes workflows; help next is read-only.

<!-- AGTOOSA PRODUCT TRUTH START: command.next -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-next` (`command.next`)
- Canonical workflow: `Docs/AgToosa_Next.md`
- Modes: `full`, `dry`, `pick`, `fix`, `test`, `docs`
- Question budget: max 0; Quick max: 0
- Mutation class: `mixed`
- Approval gate: none
- Phase stop: required
- Lifecycle close: `none`
<!-- AGTOOSA PRODUCT TRUTH END: command.next -->
"""
