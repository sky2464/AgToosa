description = "AgToosa: import external agent results and map to tasks/ACs before closure"
prompt = """

## Gemini command routing

This file is the native Gemini CLI command for `/agtoosa-import`. When the user invokes `/agtoosa-import`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read Docs/AgToosa_Import.md and execute the external agent result import workflow.

Use any argument provided to resolve the sub-command (`check`). If `check` is given, report gaps only and stop. If no argument is given, run the full import flow from Step 1.

On successful completion, print the dual-line phase close per Docs/AgToosa_Agent.md → Lifecycle Next-Step Contract
<!-- AGTOOSA PRODUCT TRUTH START: command.import -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-import` (`command.import`)
- Canonical workflow: `Docs/AgToosa_Import.md`
- Modes: `full`, `check`
- Question budget: max 0; Quick max: 0
- Mutation class: `write`
- Approval gate: none
- Phase stop: none
- Lifecycle close: `none`
<!-- AGTOOSA PRODUCT TRUTH END: command.import -->
"""
