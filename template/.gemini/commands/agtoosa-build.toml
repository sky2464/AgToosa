description = "AgToosa: break spec into atomic tasks → TDD Red-Green-Refactor → tests + SAST/DAST"
prompt = """
Read Docs/AgToosa_Build.md and execute the build workflow.

Dispatch based on any arguments provided after the command:
- /agtoosa-build        → full TDD build workflow
- /agtoosa-build scope  → task scoping phase only
- /agtoosa-build tdd    → TDD Red-Green-Refactor cycle for current task
- /agtoosa-build test   → testing and quality-gate phase only
"""
