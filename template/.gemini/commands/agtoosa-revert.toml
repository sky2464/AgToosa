description = "AgToosa: git-aware logical rollback by phase or commit"
prompt = """

## Gemini command routing

This file is the native Gemini CLI command for `/agtoosa-revert`. When the user invokes `/agtoosa-revert`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read Docs/AgToosa_Revert.md and execute the rollback workflow.

Use any argument provided after the command to identify the phase or commit to revert to (e.g. "spec", "build task 3", a commit hash, or a branch name). If no argument is given, ask the user which phase or commit to revert to before proceeding.
<!-- AGTOOSA PRODUCT TRUTH START: command.revert -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-revert` (`command.revert`)
- Canonical workflow: `Docs/AgToosa_Revert.md`
- Modes: `full`
- Question budget: max 0; Quick max: 0
- Mutation class: `write`
- Approval gate: none
- Phase stop: none
- Lifecycle close: `none`
<!-- AGTOOSA PRODUCT TRUTH END: command.revert -->
"""
