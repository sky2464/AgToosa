description = "AgToosa: diagnose hard bugs — feedback-loop → reproduce → minimise → hypothesise → instrument → fix+regress"
prompt = """

## Gemini command routing

This file is the native Gemini CLI command for `/agtoosa-debug`. When the user invokes `/agtoosa-debug`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read Docs/AgToosa_Debug.md and execute the diagnosis workflow.

Dispatch based on any arguments provided after the command:
- /agtoosa-debug                → full 6-phase diagnosis loop
- /agtoosa-debug quick          → Phases 1-3 only (feedback loop, reproduce, minimise)
- /agtoosa-debug deep           → full loop with extended instrumentation
- /agtoosa-debug feedback-loop  → Phase 1 only: establish fast feedback loop
<!-- AGTOOSA PRODUCT TRUTH START: command.debug -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-debug` (`command.debug`)
- Canonical workflow: `Docs/AgToosa_Debug.md`
- Modes: `full`
- Question budget: max 0; Quick max: 0
- Mutation class: `read-only`
- Approval gate: none
- Phase stop: none
- Lifecycle close: `none`
<!-- AGTOOSA PRODUCT TRUTH END: command.debug -->
"""
