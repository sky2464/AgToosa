description = "AgToosa: 4-persona parallel review (Security · Arch · Product · QA) + Simplifier pass"
prompt = """

## Gemini command routing

This file is the native Gemini CLI command for `/agtoosa-review`. When the user invokes `/agtoosa-review`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read Docs/AgToosa_Review.md and execute the multi-persona review workflow.

Dispatch based on any arguments provided after the command:
- /agtoosa-review           → full review: all 4 personas + Simplifier pass
- /agtoosa-review security  → Security Officer only: OWASP + STRIDE audit
- /agtoosa-review arch      → Engineering Manager only: 500-line limit, OOP, observability
- /agtoosa-review debug     → Iron Law root-cause investigation for a failing test or bug
- /agtoosa-review cross     → cross-platform second-opinion guidance
- /agtoosa-review cross-model → cross-model review gate (Docs/AgToosa_CrossModelReview.md)

Host Mode Execution (IDE Host Mode Bridge): follow Docs/AgToosa_Review.md IDE Host Mode Bridge and Docs/AgToosa_AgentCapability.md IDE Host Mode Matrix (Gemini CLI row). Use host plan equivalent for persona/findings synthesis. At briefing complete, print HOST-MODE: plan complete → switching to agent for AgToosa artifacts, then agent mode for review writes and simplification.
<!-- AGTOOSA PRODUCT TRUTH START: command.review -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-review` (`command.review`)
- Canonical workflow: `Docs/AgToosa_Review.md`
- Modes: `full`, `security`, `arch`, `debug`, `cross`, `cross-model`
- Question budget: max 0; Quick max: 0
- Mutation class: `write`
- Approval gate: required
- Phase stop: required
- Lifecycle close: `ship`
- Host mode: auto-switch true
- Plan phases: `persona_analysis`, `iron_law_hypotheses`, `cross_model_gate`, `findings_synthesis`
- Agent phases: `review_artifact_write`, `master_plan_write`, `simplification`, `verdict_gate`
<!-- AGTOOSA PRODUCT TRUTH END: command.review -->
"""
