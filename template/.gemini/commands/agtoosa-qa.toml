description = "AgToosa: plan → run → report → triage: unit, integration, E2E, browser, a11y, performance"
prompt = """

## Gemini command routing

This file is the native Gemini CLI command for `/agtoosa-qa`. When the user invokes `/agtoosa-qa`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read Docs/AgToosa_QA.md and execute the QA workflow.

Dispatch based on any arguments provided after the command:
- /agtoosa-qa           → full QA workflow
- /agtoosa-qa plan      → create QA test plan for the active spec
- /agtoosa-qa run       → execute the full test suite
- /agtoosa-qa report    → generate QA report
- /agtoosa-qa triage    → triage failing tests with Iron Law root-cause protocol
<!-- AGTOOSA PRODUCT TRUTH START: command.qa -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-qa` (`command.qa`)
- Canonical workflow: `Docs/AgToosa_QA.md`
- Modes: `full`, `plan`, `run`, `report`, `triage`
- Question budget: max 0; Quick max: 0
- Mutation class: `mixed`
- Approval gate: required
- Phase stop: required
- Lifecycle close: `review`
<!-- AGTOOSA PRODUCT TRUTH END: command.qa -->
"""
