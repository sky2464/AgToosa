description = "AgToosa: one-time project init — scan codebase, validate AI configs, create Docs/Context/ files"
prompt = """

## Gemini command routing

This file is the native Gemini CLI command for `/agtoosa-init`. When the user invokes `/agtoosa-init`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read Docs/AgToosa_Init.md and execute the initialization workflow.

Dispatch based on any arguments provided after the command:
- /agtoosa-init           → full one-time initialization: scan codebase, validate AI configs, Docs/Context/, configure AgToosa
- /agtoosa-init zoom-out  → zoom-out: call graph, module boundaries, usage sites, impact analysis for current focus area

On successful completion, print the dual-line phase close per Docs/AgToosa_Agent.md → Lifecycle Next-Step Contract
<!-- AGTOOSA PRODUCT TRUTH START: command.init -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-init` (`command.init`)
- Canonical workflow: `Docs/AgToosa_Init.md`
- Modes: `full`
- Question budget: max 12; Quick max: 0
- Mutation class: `write`
- Approval gate: required
- Phase stop: required
- Lifecycle close: `spec`
<!-- AGTOOSA PRODUCT TRUTH END: command.init -->
"""
