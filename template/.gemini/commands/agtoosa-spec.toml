description = "AgToosa: plan-mode spec interview → Executable Specification → STRIDE threat model → atomic task planning"
prompt = """

## Gemini command routing

This file is the native Gemini CLI command for `/agtoosa-spec`. When the user invokes `/agtoosa-spec`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read Docs/AgToosa_Spec.md and execute the specification workflow. Generated Project Mode — see Docs/AgToosa_Agent.md Operating Contexts (the project/product, not AgToosa as the app).

Plan-Mode Spec Interview: follow Docs/AgToosa_Spec.md Plan-Mode Spec Interview Contract (canonical). Research before asking; interview before final spec; adaptive cap 8 (quick cap 2); minimum validation floor 2 / 1; interview turn-stop after Q1.

Agent Mode Execution Contract: read Docs/AgToosa_Spec.md in full before outputs. Forbidden: skipping interview; detailed user prompt as interview-complete; writing spec files in same turn as Q1; omitting Plan-Mode Spec Interview (findings); build before approval; auto-build or auto-approve.

Host Mode Execution (IDE Host Mode Bridge): follow Docs/AgToosa_Spec.md IDE Host Mode Bridge and Docs/AgToosa_AgentCapability.md IDE Host Mode Matrix (Gemini CLI row). Use host plan equivalent or behavioral fallback. At auto-switch trigger, print HOST-MODE: plan complete → switching to agent for AgToosa artifacts, then agent mode for writes.

Dispatch based on any arguments provided after the command:
- /agtoosa-spec                   → full workflow (Parts 1 + 2 + 3 + 4: research, spec, architecture/threat-model, task planning)
- /agtoosa-spec research <topic>  → Part 1 only: context, research, domain language alignment, 6 forcing questions
- /agtoosa-spec plan              → Part 2 only: architecture blueprint + threat model
- /agtoosa-spec quick <desc>      → abbreviated: up to 2 questions + spec, skip full threat model
- /agtoosa-spec tasks             → Part 4 only: scope boundary + atomic task breakdown + test plan skeleton against an already-approved spec
- /agtoosa-spec amend             → change-control an approved spec (revision log; Must-AC changes need re-approval)
- /agtoosa-spec to-issues         → break active spec into vertical-slice GitHub issues

Phase stop: stop at the approval gate. Do not run /agtoosa-build automatically — the user must invoke it after approval.

On successful completion, print the dual-line phase close per Docs/AgToosa_Agent.md → Lifecycle Next-Step Contract
<!-- AGTOOSA PRODUCT TRUTH START: command.spec -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-spec` (`command.spec`)
- Canonical workflow: `Docs/AgToosa_Spec.md`
- Modes: `full`, `research`, `plan`, `quick`, `tasks`, `amend`, `to-issues`
- Question budget: max 8; Quick max: 2
- Mutation class: `write`
- Approval gate: required
- Phase stop: required
- Lifecycle close: `build`
- Host mode: auto-switch true
- Plan phases: `research`, `interview`, `architecture_draft`, `stride_draft`
- Agent phases: `spec_artifact_write`, `test_plan_write`, `master_plan_write`, `approval_gate`
<!-- AGTOOSA PRODUCT TRUTH END: command.spec -->
"""
