description = "AgToosa: pre-flight → deploy → archive spec → update changelog → suggest next story"
prompt = """
Read Docs/AgToosa_Ship.md and execute the ship workflow.

Dispatch based on any arguments provided after the command:
- /agtoosa-ship        → full ship workflow
- /agtoosa-ship check  → pre-flight checks only
- /agtoosa-ship docs   → archive spec + update changelog + update Master-Plan.md
- /agtoosa-ship retro  → retrospective summary + suggest next story

On successful completion, print this line verbatim: ✅ Done. Run /agtoosa-status to verify findings cleared.
"""
