description = "AgToosa: readiness gate → deploy → archive spec → update changelog → suggest next story"
prompt = """

## Gemini command routing

This file is the native Gemini CLI command for `/agtoosa-ship`. When the user invokes `/agtoosa-ship`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read Docs/AgToosa_Ship.md and execute the ship workflow.

Dispatch based on any arguments provided after the command:
- /agtoosa-ship        → full ship workflow (Part 0, then deploy approval and release steps)
- /agtoosa-ship check  → read-only readiness audit: Docs/AgToosa_Ship.md Part 0 only (no deploy, archive, or file mutation)
- /agtoosa-ship docs   → archive spec + update changelog + update Master-Plan.md
- /agtoosa-ship retro  → retrospective summary + suggest next story

On successful completion, print the dual-line phase close per Docs/AgToosa_Agent.md → Lifecycle Next-Step Contract
<!-- AGTOOSA PRODUCT TRUTH START: command.ship -->
<!-- Generated by scripts/product-truth.py; edit the contract, not this block. -->
- Command: `/agtoosa-ship` (`command.ship`)
- Canonical workflow: `Docs/AgToosa_Ship.md`
- Modes: `full`, `check`, `docs`, `retro`
- Question budget: max 0; Quick max: 0
- Mutation class: `write`
- Approval gate: required
- Phase stop: required
- Lifecycle close: `spec`
<!-- AGTOOSA PRODUCT TRUTH END: command.ship -->
"""
